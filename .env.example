# Local (same PC)
# VITE_API_URL=http://localhost:5000/api

# Local LAN (phone on same WiFi) — replace with your PC IP
# VITE_API_URL=http://192.168.x.x:5000/api

# Production (Render)
VITE_API_URL=https://raghunandan-akhada-backend.onrender.com/api

# After every Render redeploy, uploads on free plan are wiped.
# Re-sync from your PC:
#   cd server && node scripts/pushUploadsToRender.js
